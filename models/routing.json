{
  "title": "Routing",
  "description": "The purpose of this record is to maintain ROUTINGX and ROUTINGY values in the gazetteer table",
  "primaryKey": [
    "xCoordinate",
    "yCoordinate"
  ],
  "type": "object",
  "properties": {
    "uprn": {
      "typeHint": "uprn",
      "type": "integer",
      "maxLength": 12,
      "title": "UPRN",
      "description": "UPRN of the property the route serves"
    },
    "xCoordinate": {
      "typeHint": "osNatGrid",
      "type": "number",
      "maxLength": 8,
      "multipleOf": 0.01,
      "title": "X",
      "description": "X grid coordinate of access point"
    },
    "yCoordinate": {
      "typeHint": "osNatGrid",
      "type": "number",
      "maxLength": 8,
      "multipleOf": 0.01,
      "title": "Y",
      "description": "Y grid coordinate of access point"
    },
    "originHashSum": {
      "typeHint": "text",
      "type": "string",
      "title": "Hash sum",
      "description": "Hash representing column data"
    },
    "dataSource": {
      "typeHint": "text",
      "type": "string",
      "maxLength": 10,
      "title": "Data Source",
      "description": "Where this data has come from - Ordnance Survey, legacy, etc"
    }
  }
}
